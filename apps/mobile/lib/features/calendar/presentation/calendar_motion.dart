import 'package:flutter/animation.dart';

const calendarHorizontalTransitionDuration = Duration(milliseconds: 240);
const calendarInitialRevealDuration = Duration(milliseconds: 220);
const calendarCellContentRevealDuration = Duration(milliseconds: 180);
const calendarContentRevealCurve = Curves.easeOutCubic;
